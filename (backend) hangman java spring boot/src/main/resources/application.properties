spring.application.name=stickman
# use PORT env (Railway provides $PORT) with fallback 8091 for local dev
server.port=${PORT:8091}
# frontend origin. Set FRONTEND_URL on Railway to your deployed frontend domain.
# default is local dev URL (fixed scheme)
frontend.url=${FRONTEND_URL:http://localhost:5173}
